# Dependencies
node_modules
pnpm-lock.yaml

# Build outputs
dist

# Fonts
public/fonts

# Copied verbatim from the desktop app.
#
# `check-drift` compares these byte-for-byte, so formatting them here would fail
# it — which is what happened the first time this file did not exist. They are
# not edited here at all: change them upstream and copy them across.
src/reader-core

# The default summary prompt, kept as authored.
#
# Prose that is sent to a model verbatim, not documentation about it. Prettier
# reflows markdown — it rewrites `*Bad:*` as `_Bad:_` and inserts blank lines —
# and every one of those characters ends up in the request.
AI-video-viewer-default-initial-summary-prompt.md

# Misc
.DS_Store
*.log

# Screenshot output from scripts/shoot.py
dev-shots
